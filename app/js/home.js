/**
 * @jsx React.DOM
 */
var Home = React.createClass({

  render: function() {
    return (
      <div className="inner cover">
            <h1 className="cover-heading">Generate your website</h1>
            <p className="lead">Lisa is a boilerplate SPA project that comes with  React/Flux/ and a custom router.</p>
      </div>
    );
  }

});

module.exports = Home;